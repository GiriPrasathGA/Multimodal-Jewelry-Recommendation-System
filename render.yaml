services:
  - type: web
    name: jewelux-backend
    runtime: docker
    dockerContext: backend
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /health
