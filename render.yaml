services:
  - type: web
    name: jewelux-backend
    id: jewelux-backend
    runtime: docker
    rootDirectory: backend
    dockerfilePath: Dockerfile
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: "10000"
    healthCheckPath: /health
